Script downloading student marks from adjule.

### Requirements:
* Chrome or Chromium browser
* chromedriver
* selenium
* dateparser
* progressbar2

### Installation:
* clone repo
* run `conda env create` or install requirements manually

### Usage:
* type `./adjule_points.py -h` on Unix (similarly on Windows)

